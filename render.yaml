services:
  - type: web
    name: venda-facil
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:create_app()"
    # envVars:
      # - key: FLASK_ENV
      #   value: production
      # - key: DATABASE_URL
      #   value: sua_string_de_conexao_postgres